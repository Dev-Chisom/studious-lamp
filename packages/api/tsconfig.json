{
	"extends": "../../tsconfig.json",
	"compilerOptions": {
		"outDir": "./dist",
		"rootDir": "./src",
		"baseUrl": ".",
		"paths": {
			"@whispers/types": ["../types/dist"] // ← Point to DIST instead of SRC
		}
	},
	"include": ["src/**/*"],
	"references": [{ "path": "../types" }]
}
